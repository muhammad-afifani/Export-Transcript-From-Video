# Install semua sekaligus (core + diarization + OCR):
#   pip install -r requirements.txt
# Untuk instalasi ringan/cepat (hanya transcript + MoM), pakai requirements-core.txt saja.

-r requirements-core.txt
-r requirements-diarization.txt
-r requirements-ocr.txt
